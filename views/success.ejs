<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Success</title>
</head>
<body>
    <h2>Registration Successful</h2>
    <p>Your account has been successfully registered. Please save the following details and keep them safe:</p>
    <table>
        <tr>
            <td>Username:</td>
            <td><%= username %></td>
        </tr>
        <tr>
            <td>Email:</td>
            <td><%= email %></td>
        </tr>
        <tr>
            <td>Password:</td>
            <td><input type="password" id="password" value="<%= password %>" readonly>
                <button onclick="togglePasswordVisibility()">Show/Hide</button>
            </td>
        </tr>
    </table>

    <a href="/login">Proceed to Login</a>

    <script>
        function togglePasswordVisibility() {
            const passwordInput = document.getElementById("password");
            if (passwordInput.type === "password") {
                passwordInput.type = "text";
            } else {
                passwordInput.type = "password";
            }
        }
    </script>
</body>
</html>
